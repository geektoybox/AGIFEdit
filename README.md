Geek's GIF Editor (PyQt6)
================================

Requirements
------------

- Python 3.9+
- PyQt6
- Pillow (PIL)

Install dependencies:

    pip install PyQt6 Pillow

Run the app:

    python geeks_gif_editor.py
